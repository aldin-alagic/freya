import styled from 'styled-components';
import { boxShadow } from '../../lib/style/theme';

export const SolutionWrapper = styled.div``;

export const Inner = styled.div`
  box-shadow: ${boxShadow};
`;
